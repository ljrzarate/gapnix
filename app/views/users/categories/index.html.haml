%h1 My Categories
%br/

- if @categories.empty?
  = render "users/categories/empty_state"
- else
  #{link_to "Create new category", new_user_project_path, class: 'btn btn-default'}
  = render "users/categories/category_list", categories: @categories
